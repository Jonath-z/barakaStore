
export const redirectToGallerie = (history) => {
    history.push('/Gallerie');
}
export const redirectToAcceuil = (history) => {
    history.push('/');
}
export const redirectToCatalogue = (history) => {
    history.push('/Catalogue');
}
export const redirectToSouscire = (history) => {
    history.push('/Souscrire');
}
export const redirectToCommande = (history) => {
    history.push('/passer-une-commande')
}